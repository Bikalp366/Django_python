# Python Django Stuff – Day 1

Started a Django project today.  
Made a `home.html` page, connected it with `views.py` and `urls.py`, and pushed it to GitHub.  
Ignored `.venv/` and `db.sqlite3`.  

Run it with:
```bash
git clone https://github.com/Bikalp366/python_django_stuff.git
cd python_django_stuff
python3 -m venv .venv
source .venv/bin/activate
pip install django
python manage.py runserver
